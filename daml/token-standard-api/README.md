# Token Standard

This directory contains `daml` files from the [Splice Token Standard](https://github.com/hyperledger-labs/splice/tree/main/token-standard) DAML libraries. The `yaml` files can be found in `@openapi` (see [tsconfig.json](../tsconfig.json))
