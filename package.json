{
  "name": "test-coin",
  "module": "index.ts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "daml:rebuild": "(cd ./daml && rm -rf .daml && daml clean && daml build) && (cd ./daml/test && rm -rf .daml && daml clean && daml build)",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "openapi-backend": "^5.15.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
